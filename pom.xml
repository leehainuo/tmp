<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.miku</groupId>
    <artifactId>miku</artifactId>
    <version>0.0.1</version>

    <name>miku</name>
    <url>https://www.lihainuo.com</url>
    <description>Miku 一个优雅的后台框架</description>

    <properties>
        <miku.version>0.0.1</miku.version>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <jwt.version>0.13.0</jwt.version>
        <knife4j.version>4.5.0</knife4j.version>
        <springdoc.version>2.8.14</springdoc.version>
        <mybatis-plus.version>3.5.9</mybatis-plus.version>
        <druid.version>1.2.22</druid.version>
    </properties>

    <!--
     | Spring Boot
     |
     | Spring Boot Parent 是官方提供的 "父 POM", 作用：
     |
     | 1. 统一编码、Java 版本、资源过滤、插件
     | 2. 包含 Spring Boot 生态常用依赖的版本管理, 无需手动管理版本
     | 3. 规范化构建, 默认开启一些 Maven 约定, 让项目 "开箱即用"
     |-->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.7</version>
        <relativePath/>
    </parent>

    <dependencyManagement>
        <dependencies>
            <!-- Modules -->
            <dependency>
                <groupId>com.miku</groupId>
                <artifactId>miku-cmd</artifactId>
                <version>${miku.version}</version>
            </dependency>
            <dependency>
                <groupId>com.miku</groupId>
                <artifactId>miku-mod</artifactId>
                <version>${miku.version}</version>
            </dependency>
            <dependency>
                <groupId>com.miku</groupId>
                <artifactId>miku-pkg</artifactId>
                <version>${miku.version}</version>
            </dependency>
            <dependency>
                <groupId>com.miku</groupId>
                <artifactId>miku-core</artifactId>
                <version>${miku.version}</version>
            </dependency>
            <dependency>
                <groupId>com.miku</groupId>
                <artifactId>miku-demo</artifactId>
                <version>${miku.version}</version>
            </dependency>
            <!-- Starters -->
            <dependency>
                <groupId>com.miku</groupId>
                <artifactId>miku-starter-file</artifactId>
                <version>${miku.version}</version>
            </dependency>
            <dependency>
                <groupId>com.miku</groupId>
                <artifactId>miku-starter-excel</artifactId>
                <version>${miku.version}</version>
            </dependency>
            <dependency>
                <groupId>com.miku</groupId>
                <artifactId>miku-starter-redis</artifactId>
                <version>${miku.version}</version>
            </dependency>

            <!-- JWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <!-- Knife4j -->
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
                <exclusions>
                    <exclusion>
                        <groupId>org.springdoc</groupId>
                        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <!-- MyBatis Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-bom</artifactId>
                <version>${mybatis-plus.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Druid -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-3-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <packaging>pom</packaging>
    <modules>
        <module>miku-cmd</module>
        <module>miku-mod</module>
        <module>miku-pkg</module>
        <module>miku-core</module>
        <module>miku-starter</module>
    </modules>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <!-- 
                     | 按需开启：开启依据为是否有本地 Jar 必须随最终可执行包一起发布
                     | 
                     | 详细解释：假设你的项目里有个第三方 Jar 包只能手动放在 libs/demo.jar, 并且必须在运行时存在
                     |         那么这种情况下, 需要开启此选项.
                     |
                     | <configuration>
                     |     <includeSystemScope>true</includeSystemScope>
                     | </configuration>
                     |-->
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
